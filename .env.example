# Copy to .env.local for a local build, or set it in the Vercel project.
#
# A Cathode server for every visitor. For development only: the tests set it.
# Leave it empty on a public page, so each person adds their own server.
VITE_CATHODE_SERVER=
