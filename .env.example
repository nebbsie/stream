# Copy to .env.local for a local build, or set it in the Vercel project.
#
# The Cathode server this page offers. Leave it empty and every space is peer
# to peer unless somebody types a server address in by hand.
VITE_CATHODE_SERVER=
